backend:
  name: github
  branch: master # Branch to update (optional; defaults to master)
media_folder: "assets"
collections:
  - name: "media"
    label: "Media Item"
    folder: "_media"
    fields:
      - {label: "Title"}
